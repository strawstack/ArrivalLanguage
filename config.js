let f = 1.5;

let config = {
    center: {x: 512, y: 512}, // center of character
    radius: 300, // Radius of character,
    blobRadius: {
        small: 20 * f,
        medium: 40 * f,
        large: 80 * f
    },
    strokeLength: (80 * f) + 10
};
